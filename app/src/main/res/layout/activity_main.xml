<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_background_color"
    android:orientation="vertical"
    android:paddingHorizontal="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="40dp"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/primary_text_size" />

    <Button
        android:id="@+id/button_search"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_button_layout_height"
        android:layout_marginBottom="@dimen/main_button_margin_bottom"
        android:layout_weight="1"
        android:backgroundTint="@color/white"
        android:text="@string/button_title_search"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:textSize="@dimen/primary_text_size"
        app:cornerRadius="@dimen/main_button_corner_radius"
        app:icon="@drawable/ic_search"
        app:iconGravity="textStart"
        app:iconTint="@color/black"></Button>

    <Button
        android:id="@+id/button_media"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_button_layout_height"
        android:layout_marginBottom="@dimen/main_button_margin_bottom"
        android:layout_weight="1"
        android:backgroundTint="@color/white"
        android:text="@string/button_title_media"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:textSize="@dimen/primary_text_size"
        app:cornerRadius="@dimen/main_button_corner_radius"
        app:icon="@drawable/ic_media"
        app:iconGravity="textStart"
        app:iconTint="@color/black">

    </Button>

    <Button
        android:id="@+id/button_settings"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_button_layout_height"
        android:layout_marginBottom="@dimen/main_button_margin_bottom"
        android:layout_weight="1"
        android:backgroundTint="@color/white"
        android:text="@string/button_title_settings"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:textSize="@dimen/primary_text_size"
        app:cornerRadius="@dimen/main_button_corner_radius"
        app:icon="@drawable/ic_settings"
        app:iconGravity="textStart"
        app:iconTint="@color/black">

    </Button>

</LinearLayout>
